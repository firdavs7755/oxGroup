import {createAction} from '@reduxjs/toolkit'

export const SET_DATA = createAction('SET_DATA');
export const GET_DATA = createAction('GET_DATA');
export const SET_TOKEN = createAction('SET_TOKEN');

